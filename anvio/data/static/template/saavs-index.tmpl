<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Anvi'o">
    <base target="_blank">

    <title>SAAVs and Protein Structures</title>

    <!-- Bootstrap Core CSS -->
    <link href=".html/css/bootstrap.css" rel="stylesheet">
    <link href=".html/bootstrap-sortable/Contents/bootstrap-sortable.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href=".html/css/anvio.css" rel="stylesheet">
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="http://merenlab.org/projects/anvio">Anvi'o Project Page</a>
                    </li>
                    <li>
                        <a href="https://github.com/meren/anvio">Anvi'o Repository</a>
                    </li>
                    <li>
                        <a href="https://github.com/meren/anvio/issues">Report an Issue</a>
                    </li>
                    <li>
                        <a href="http://merenlab.org/people">Contact</a>
                    </li>
                    <li>
                        <a href="https://github.com/meren/anvio/releases">Anvi'o version: {{ meta|lookup:"anvio_version" }}</a>
                    </li>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>


    <header class="image-bg-fluid-height">
        <div class="header-summary-div">
            <div style="width: 200px;">
                <img class="img-responsive img-left"src=".html/pics/logo.png" alt="">
            </div>
            <div class="header-text">
                Welcome to the anvi'o summary output for the single-amino acid variants and protein structures. This one seems to have {{ data|lookup:"gene_names" | length }} proteins across {{ data|lookup:"samples"|length }} samples. {% if meta|lookup:"images_soft_linked" %}<b>Warning</b>: The images in this output are soft linked, which means the output directory will only work on this computer.{% endif %}
            </div>
        </div>
    </header>


    {% for gene in data|lookup:"gene_names" %}
    <section>
        <div class="container-fluid">
            <div class="panel panel-info" id="{{ gene }}_panel">

                <div class="panel-heading">
                    <h1 class="panel-title">
                        <a data-toggle="collapse" data-parent="#{{ gene }}_panel" href="#collapse-{{ gene }}">{{ gene }}</a> |
                        <a href=https://www.google.com/search?q={{ data|lookup:"genes"|lookup:gene|lookup:"function" }} target="_blank">{{ data|lookup:"genes"|lookup:gene|lookup:"function" }}</a> |
                        <a href=http://pfam.xfam.org/family/{{ data|lookup:"genes"|lookup:gene|lookup:"accession" }} target="_blank">{{ data|lookup:"genes"|lookup:gene|lookup:"accession" }}</a>
                    </h1>
                </div>

                <div id="collapse-{{ gene }}" class="panel-collapse">
                    <div class="panel-body">

                        {% for view in data|lookup:"samples_per_view" %}
                        <div class="panel panel-default">
                        <div class="panel-heading">
                            <h1 class="panel-title"><a data-toggle="collapse" data-parent="#{{ gene }}-{{ view }}_panel" href="#collapse-{{ gene }}-{{ view }}">{{ view }}</a></h1>
                        </div>

                        <div id="collapse-{{ gene }}-{{ view }}" class="panel-collapse collapse">
                        <div class="panel-body">
                        <div class="panel panel-default">
                            <div class="panel-body">
                                <table class="table table-striped sortable" id="tblGrid_bin">
                                    <tbody>
                                        {% for variable in data|lookup:"views_and_variables"|lookup:view %}
                                        <tr>
                                            <td style="vertical-align: middle; width: 10px;"><b>{{ variable }}</b></td>
                                            {% for sample in data|lookup:"samples_per_view_with_padding"|lookup:view|lookup:variable %}
                                                {% if data|lookup:"by_view"|lookup:gene|lookup:view|lookup:variable|lookup:sample %}
                                                <td style="vertical-align: middle;"><img src="{{ data|lookup:"by_view"|lookup:gene|lookup:view|lookup:variable|lookup:sample }}" width="130px;" /></td>
                                                {% else %}
                                                <td style="vertical-align: middle;">&nbsp;</td>
                                                {% endif %}
                                            {% endfor %}
                                        </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        </div>
                        </div>
                        </div>
                        {% endfor %}

                        </div>
                    </div>
                </div>

            </div>
        </div>
    </section>
    {% endfor %}



    <div style="margin-bottom:200px"></div>

    <!-- jQuery -->
    <script src=".html/js/jquery.min.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src=".html/js/bootstrap.min.js"></script>

    <!-- Table sorter, source: https://github.com/drvic10k/bootstrap-sortable -->
    <script src=".html/bootstrap-sortable/Scripts/bootstrap-sortable.js"></script>

    <!-- Activate tooltips... -->
    <script type="text/javascript">
    $(document).ready(function() {
        $("body").tooltip({ selector: '[data-toggle=tooltip]' });
    });
</script>
</body>
</html>
