<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Anvi'o">

    <title>Anvi'o Output for PROJECT</title>

    <!-- Bootstrap Core CSS -->
    <link href=".html/css/bootstrap.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href=".html/css/full-width-pics.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

</head>

<body>

    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="https://github.com/meren/anvio">Anvi'o Repository</a>
                    </li>
                    <li>
                        <a href="https://github.com/meren/anvio/blob/master/README.md">User Manual</a>
                    </li>
                    <li>
                        <a href="https://github.com/meren/anvio/issues">Report an Issue</a>
                    </li>
                    <li>
                        <a href="#">Contact</a>
                    </li>
                    <li>
                        <a href="#">Anvi'o version: {{ meta|lookup:"profile"|lookup:"version"  }}</a>
                    </li>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>

    <!-- Image backgrounds are set within the full-width-pics.css file. -->
    <header class="image-bg-fluid-height">
        <div class="header-summary-div">
            <div style="width: 200px;">
                <img class="img-responsive img-left"src=".html/pics/logo.png" alt="">
            </div>
            <div class="header-text">
                Bins that were identified in {% if meta|lookup:"profile"|lookup:"merged" == '1' %} merged {% else %} single {% endif %} profile '<b>{{ meta|lookup:"profile"|lookup:"sample_id"|humanize }}</b>' and stored in the database as "<b>{{ meta|lookup:"collection_id"|humanize }}</b>" collection, describe <b>{{ meta|lookup:"num_groups"|pretty }}</b> groups that resulted in <b>{{ meta|lookup:"num_contigs"|pretty }}</b> contigs in total accountig for <b>{{ meta|lookup:"total_length"|pretty  }}</b> nucleotides, which represent <b>{{ meta|lookup:"percent_described" }}%</b> of the original assembly for this project stored in the annotation database.
            </div>
        </div>
    </header>

    {% for group_id in meta.groups %}
    <!-- Content Section -->
    <section>
        <div class="container" style="border-left: 25px {{ groups|lookup:group_id|lookup:"color" }} solid; border-radius: 25px; background-color: rgba(200, 200, 200, .2);">
            <div class="row">
                <div class="col-lg-12">
                    <h2 class="section-heading">{{ group_id|humanize }} <span style="opacity: 0.4">[{{ groups|lookup:group_id|lookup:"total_length"|humanize_n }}]</span></h2>
                    <div class="group-tag-line">
                            <span style="color: rgb(16, 73, 16); font-size: larger;">~{{groups|lookup:group_id|lookup:"percent_complete"|humanize_n}}%</span> complete,
                            <span style="color: rgb(73, 19, 19); font-size: larger;">~{{groups|lookup:group_id|lookup:"percent_contamination"|humanize_n}}%</span> contaminated.
                    </div>

                    <p class="lead section-lead">Downloads:</p>
                    <p class="section-paragraph"><a href="{{ groups|lookup:group_id|lookup:"files"|lookup:"contigs"   }}">Fasta File</a></p>
                </div>
            </div>
            <!-- /.row -->
        </div>
        <!-- /.container -->
    </section>
    {% endfor %}

    <!-- jQuery -->
    <script src="js/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>

</body>

</html>
